
<!DOCTYPE html>
<html lang="en" class="ui-toolkit">
<head>
    <title> Lenil - Base de connaissances </title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Plateforme des frrelancers africains.">
    <meta name="keywords" content="lenil,freelancer, agents, services, online marketplace, professional services, fiverr, people per hour, upwork,">
    <meta name="author" content="Waarbi">
    <link href="https://fonts.googleapis.com/css?family=Roboto:400,500,700,300,100" rel="stylesheet">
    <link href="{{ asset('build/css/bootstrap_css.css') }}" rel="stylesheet">
    <link href="{{ asset('build/css/custom_css.css') }}" rel="stylesheet">
    <link href="{{ asset('build/css/animate_css.css') }}" rel="stylesheet">
    <link href="{{ asset('build/css/styles.css') }}" rel="stylesheet">
    <link href="{{ asset('build/css/knowledge_base_css.css') }}" rel="stylesheet">
    <link href="{{ asset('build/css/categories_nav_styles.css') }}" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <link href="{{ asset('build/css/owl_carousel_css.css') }}" rel="stylesheet">
    <link href="{{ asset('build/css/easy_autocomplete_css.css') }}" rel="stylesheet">
    <link href="{{ asset('build/css/owl_theme_default_css.css') }}" rel="stylesheet">
    <link href="{{ asset('build/css/sweat_alert_css.css') }}" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">

    <link rel="shortcut icon" href="{{ asset('build/images/logo-ico.ico') }}" type="image/x-icon"/>
    <style>
        .form-control{
            width: 200px;
        }
    </style>
</head>
<body class="is-responsive">
<div class="header" style="">
    <div class="container">
        <a class="navbar-brand logo text-success" href="{{ path('homepage') }}">
            <img src="{{asset('build/images/logo.PNG')}}" width="100" style="margin-top:8%;">
        </a>
        <div class="text-center">
            <h2 class="text-white mt-5">BANQUE DE CONNAISSANCES POUR LENIL</h2>
            <h4 class="text-white">Tout ce que tu as besoin de savoir</h4>
        </div>
        <div class="text-center reduceForm">
            <form action="{{ path('guide_pratique')}}" method="post">
                <div class="input-group space50">
                    <input type="text" name="search_query" class="form-control" value="" required  placeholder="Recherche une question">
                    <div class="input-group-append move-icon-up" style="cursor:pointer;">
                        <button name="search_article" type="submit" class="search_button">
                            <img src="{{ asset('build/images/srch2.png') }}" class="srch2">
                        </button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>
<div class="container mt-5 mb-5">
    <div class="row" style="">
    {% for articleCategorie in articlesCatogories %}
        <div class="col-md-6">
            <h3 class="make-black pb-1"><i class="fa fa-bars"></i> {{ articleCategorie.title }} </h3>
            <!-- Category -->
            {% for article in articleCategorie.articles %}
                <h5><a href="{{ path('show_article',{slug: article.slug }) }}" class="text-success"><i class="fa fa-book"></i> {{ article.title }} </a></h5><br>
            {% else %}
                <p>Aucun article à afficher pour le moment.</p>
            {% endfor %}
        </div>
    {% else %}
        <div class="col-md-6">
            <h4 class="make-black pb-1"><i class="fa fa-bars"></i>Aucun article pour le moment </h4>
        </div>
    {% endfor %}
    </div>
</div>
<!-- start footer -->
{{ include('partials/footer.html.twig') }}
</body>
</html>